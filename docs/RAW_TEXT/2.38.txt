This exercise requires to give the correct value of "op" in order to make the expression's values below equal with fold-right and fold-left. fold-right is like accumulate, fold-left combines elements in the opposite direction:
(define (fold-left op initial sequence)
    (define (iter result rest)
        (if (null? rest)
            result
            (iter (op result (car rest)) (cdr rest))))
    (iter initial sequence))

(define (fold-right op initial sequence)
        (if (null? sequence)
            initial
            (op (car sequence) (fold-right op initial (cdr sequence))))
)


(fold-right / 1 (list 1 2 3)) (/ 1 (/ 2 (/3 1)) = 1/2/3 = 3/2
(fold-left / 1 (list 1 2 3)) 1/1/2/3 = (/ 1 (/1 (/ 2 3)) = 2/3 
(fold-right list '() (list 1 2 3)) (1 (2 (3 '())))
(fold-left list '() (list 1 2 3)) ((('() 1) 2) 3)

TEST RESULT --- NEGATIVE
right, wrong, right, right.

The second expression gives 1/6 because I made a mistake in the order of calculation.

What op should be to make the results equal between fold-right and fold-left?
consider:
(lambda (x y) (/ y x))?
(lambda (x y) (list y x))?

op should be commutative of course