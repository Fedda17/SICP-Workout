This exercise asks me to combine cars and cdrs in order to pick "7" from the lists in the text

1: cadddr
2: caar
3: cadddddddr

TEST RESULT --- NEGATIVE
first wrong
second right
third wrong

The first one needs to pick the third element: I missed a car
The third one the nested expression will require that I'll pick the second element combining d and a alternating I guess

1: cadaddr-> (car (cdr (car (cdr (cdr ... )))))
3: cadadadadadadr-> (car (cdr (car (cdr (car (cdr (car (cdr (car (cdr (car (cdr ...))))))))))))

TEST RESULT --- 
third right
first right

end exercise.

