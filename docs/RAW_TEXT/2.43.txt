If I change the previous program this way:

(flatmap
    (lambda (new-row)
        (map (lambda (rest-of-queens)
                    (adjoin-position new-row k rest-of-queens))
                (queen-cols (- k 1))))
    (enumerate-interval 1 board-size))

the computation does differently:
first generate the sets to place k - 1 queens in the columns.
Then, for each of these sets, put a new row for the kth queen. And then filter all the solutions.
And finally filter the sets. I guess the filter operation takes more time to go through all the sets?

Let's see:

(define (queens board-size)
    (define (queen-cols k)
        (if (= k 0)
            (list empty-board)
            (filter
                (lambda (positions)
                    (safe? k positions))
                (flatmap
                    (lambda (new-row)
                            (map (lambda (rest-of-queens)
                                    (adjoin-position new-row k rest-of-queens))
                            (queen-cols (- k 1))))
                    (enumerate-interval 1 board-size)))))
    (queen-cols board-size))

By tracing the operation, I see that for every set, it goes the trouble to re-check the positions for k - 1.
So if T is the time it takes in the previous implementation, I guess the new time is T^t