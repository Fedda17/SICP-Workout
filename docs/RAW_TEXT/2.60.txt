If sets are represented by normal lists, how this affect the performance of the program when I change the selectors and constructors accordingly?

(define (element-of-set? x set)
    (cond 
        ((null? set) #f)
        ((eq? (car set) x) #t )
        (else (element-of-set x (cdr set))))
)

(define (adjoin-set x set) (cons x set))

(define (union-set set1 set2) (append set1 set2))

(define (intersection-set set1 set2)
    (cond 
        ((null? set1) '())
        ((element-of-set? (car set1) set2) (adjoin-set (car set1) (instersection-set (cdr set1) set2)))
        (else (intersection-set (cdr set1) set2)))
)

The main takeway is that it's the resource of space increase with this representation, but the numbers of steps for elements-of-set and intersection-set is the same, adjoin-set is constant time. union-set is n time.
I don't have in mind applications for which representation would be more convenient over the one that doesn't admit duplicates, but I think it will depend how the system depends on adjoin-set and union-set?