# 2.34

## Follow Up
This exercise asks me to use accumulation procedure defined below to implement the Horner's rule 
the coefficient are in a sequence (let's say list) in order from a0 to an

``` racket
(define (accumulate op initial sequence)
    (if (null? sequence)
        initial
        (op (car sequence)
            (accumulate op
                    initial
                    (cdr sequence)))))
```


Well, let's complete the procedure:

## First Attempt
I notice that the Horner's rule is recursive, so I have to find the recursive step

``` racket
(define
    (horner-eval x coefficient-sequence)
        (accumulate
            (lambda (this-coeff higher-terms)
                (+ this-coeff (* x (horner-eval x higher-terms))))
            0
            coefficient-sequence))
```


I have to test it with this value:  
 1 + 3x + 5x^3 + x^5 at x = 2  

 TEST RESULT --- NEGATIVE  
 car: contract violation  
  expected: pair?  
  given: 0  


## Second Attempt 
``` racket
  (define
    (horner-eval x coefficient-sequence)
        (accumulate
            (lambda (this-coeff higher-terms)
                (+ this-coeff (* x  higher-terms)))
            0
            coefficient-sequence))
```

>(horner-eval 2 (list 1 3 0 5 0 1))  
79

Correct! I mean TEST RESULT --- AFFIRMATIVE. The previous definition was wrong for a misunderstanding of the accumulate definition.
