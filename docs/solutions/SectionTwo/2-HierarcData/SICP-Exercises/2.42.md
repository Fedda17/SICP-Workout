# 2.42
## Follow Up
This exercise asks me to solve the "eight-queen puzzle" using sequence operations.  
A solution that the exercise propose is to place a queen in each column. I have "k" queens so, for each column.
When I place the k - (1 queens, I must place the kth queen in this approach:  
(the chess board is a square)
When I have all the possible placing of the k - 1 queens in the first k - 1 columns, for each of these ways, I generate an extendend set of positions by placing a queen in each row of the kth column.
Then I have to filter the safe positions for the queen in the kth column, this produce a sequence of all the ways to place k queens in the first k column.
Recursively so, we get all the solutions of the puzzle.

Implementation:
``` racket
(define (queens board-size)
    (define (queen-cols k)
        (if (= k 0)
            (list empty-board)
            (filter
                (lambda (positions)
                    (safe? k positions))
                (flatmap (lambda (rest-of-queens)
                            (map (lambda (new-row) (adjoin-position new-row k rest-of-queens))
                                  (enumerate-interval 1 board-size)))
                        (queen-cols (- k 1))))))
    (queen-cols board-size))
```

The text explains:
rest-of-queens is a way to place k - 1 queens in the first k - 1 columns.
new-row is a proposed to row in which to place the queen for the kth column.

I have to complete the implementation by defining the missing procedures such as:
adjoin-position, empty-board and safe?

for adjoin-position, takes as an input the set of position and a row column, and returns the adjoined set.
empty-board represent a set of empty positions.
safe? takes as an input the k queen and the set of position and returns true if the queen is safe in that position.


## First Attempt
Well, let's start by giving a concrete representation of a set, let's keep it simple and implementing it with an unordered-list. I choose to represent the empty set with an empty list as well.

``` racket
(define (make-position r c) (cons r c))

(define (get-row p) (car p))

(define (get-column p) (cdr p))

(define empty-board '())

(define (adjoin-position row column set)
    (define (iter iter-set) 
    (cond  ((null? set) (cons (make-position row column) set))
            ((and (= row (get-row (car set))) (= column (get-column (car set)))) set)
            (else (adjoin-position row column set (cdr set)))))
)

(define (safe? column positions)
    (cond ((null? positions) #t)
          ((= column (get-column (car positions))) #f)
          (else (safe? column (cdr positions))))
)
```


TEST RESULT --- NEGATIVE

> (queens 5)  
'()  
> (queens 2)  
'()  
> (queens 10)  
'()  
>   

Adjoin position doesn't work.
I understand, I take the set cdr as an adjoining set and so it always builds a set of one element.

## Second Attempt
``` racket
(define (adjoin-position row column positions)
    (define (iter iter-positions) 
        (cond  ((null? iter-positions) (cons (make-position row column) positions))
            ((and (= row (get-row (car iter-positions))) (= column (get-column (car iter-positions)))) positions)
            (else (iter (cdr iter-positions)))))
    (iter positions))

(define (safe? column positions)
    (cond ((null? positions) #t)
          ((= column (get-column (car positions))) #f)
          (else (safe? column (cdr positions))))
)
```

## Safe? solution

Now then, How can I check when a new queen k is safe in respect to other position.
k represents each queen in the column k, and positions are the previously placed safe queens.
take the column k, then add the positions for the column k to the set. Each queen position for a k column reside in another set, then it flatmaps the solution.
to combine them.

Try with multiplication? Better with sum:  
Nope.

``` racket
(define (adjoin-position row column positions)
    (cons (make-position row column) positions)
)

(define (safe? column positions)
    (let ((k-queen (list-ref (filter 
                                (lambda (pos) (= column (get-column pos))) positions) 
                            0))
            (sums (map (lambda (pos) 
                            (+ (get-column pos) (get-row pos))) positions))
            (differences (map (lambda (pos) 
                            (- (get-row pos) (get-column pos))) positions)))
        (cond ((= column 0) #t)
                ((or 
                    (> (length (filter (lambda (pos) (= (get-row pos) (get-row k-queen))) positions)) 1)
                    (> (length (filter (lambda (sum) (= sum (+ (get-row k-queen)  (get-column k-queen)))) sums)) 1)
                    (> (length (filter (lambda (difference) (= difference (- (get-row k-queen)  (get-column k-queen)))) differences)) 1))
                    #f)
        (else #t))
    )
)
```

