# 2.35

## Follow Up
This exercise asks me to redefine count-leaves as an accumulation

## Pivot
``` racket
    (define (count-leaves t)
        (accumulate + 0 (map (lambda (sub-tree) (if (pair? sub-tree) (count-leaves sub-tree) 1)) t)))
``` 

test data   
(define x (cons (list 1 2) (list 3 4)))  

TEST RESULT --- AFFIRMATIVE  
>(count-leaves x)  
4